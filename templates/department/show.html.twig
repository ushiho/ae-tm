{% extends 'department/departmentBase.html.twig' %}
{% block center_column %}
{#<div class="top-bar">
    <a title="Add new Department" href="/department/new" class="button">ADD NEW
    </a>
    <h1>Department</h1><br>
    <div class="breadcrumbs">#}
{% block path %}
   {# {{ parent()}} / <a href="#" class="path"> Details</a>#}
{% endblock %}
{#
</div>
</div>
<hr><br>
#}
    <div class="panel-body">

        <div class="panel-heading main-color-bg">
            <h3 class="panel-title">{{ department.name }}'s Details</h3>
        </div>

        <div class="table-responsive">
            <br>
            <table class="table table-striped table-hover" cellpadding="0" cellspacing="0">
                    <tr>
                        <td class="first" width="172"><strong>Name</strong></td>
                        <td class="last">{{ department.name }}</td>
                    </tr>
                    <tr class="bg">
                        <td class="first"><strong>Address</strong></td>
                        <td class="last">{{ department.adress }}</td>
                    </tr>
                    <tr class="bg">
                        <td class="first"><strong>Missions</strong></td>
                        <td class="last">{% if department.missions and department.missions is not empty %} 
                        <a href="{{ path('showMissionOfDepartment', {'idDepartment':department.id}) }}" class="link">Show </a>
                        {% else %} Add Mission {% endif %}</td>
                    </tr>
            </table>
        </div>
        <div class="btn-group btn-group-justified">
            <a title="Show All Rents" href="{{ path('allDepartments') }}" class="btn btn-success btn-xs glyphicon glyphicon-arrow-left"> Back </a>
            {% if connectedUser.role == 1 %}
            <a title="Edit This Rent" href="{{ path('editDepartment', { 'id' : department.id }) }}" class="btn btn-primary btn-xs glyphicon glyphicon-pencil"> EDIT </a>
            {% endif %}
        </div>
    </div>
{% endblock %}
